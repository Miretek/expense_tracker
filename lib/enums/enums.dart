enum ExpenseCategory { food, entertainment, rent, gas, clothes, taxi, other }

enum IncomeCategory { salary, deposit, loan, other }

// ignore: constant_identifier_names
enum Currency { ETB, USD, EUR }
